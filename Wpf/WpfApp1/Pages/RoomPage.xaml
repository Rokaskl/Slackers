<Page x:Class="WpfApp1.Pages.RoomPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RoomPage">

    <Grid Name ="pageGrid" Margin="0,0,-400,0">
        <Button x:Name="btnLogoutRoom" Content="Logout" HorizontalAlignment="Left" Margin="676,38,0,0" VerticalAlignment="Top" Width="75" Click="BtnLogoutRoom_Click"/>
        <Button x:Name="btnStartStop" Content="Start!" HorizontalAlignment="Left" Margin="656,374,0,0" VerticalAlignment="Top" Width="95" RenderTransformOrigin="0.336,-0.495" Height="41" Click="btnStartStop_Click"/>
        <ListView x:Name="MembersListView" HorizontalAlignment="Left" Height="377" Margin="36,38,0,0" VerticalAlignment="Top" Width="588">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Members"
                                DisplayMemberBinding="{Binding [username]}" />
                </GridView>
            </ListView.View>
        </ListView>
        <ComboBox x:Name="cmbStatus" HorizontalAlignment="Left" Margin="656,114,0,0" VerticalAlignment="Top" Width="120"/>
        <Border BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Left" Height="393" Margin="776,22,0,0" VerticalAlignment="Top" Width="0"/>
        <Button x:Name="btn_addnote" Content="Add" HorizontalAlignment="Left" Margin="1088,400,0,0" VerticalAlignment="Top" Width="75" Click="Btn_addnote_Click"/>
        <ListView x:Name="NoteListView" HorizontalAlignment="Left" Height="372" Margin="810,10,0,0" VerticalAlignment="Top" Width="380">
            <ListView.View>
                <GridView>
                    <GridViewColumn
                                DisplayMemberBinding="{Binding Path=Id}"/>
                    <GridViewColumn Width="300">
                        <!--<TextBox Text="{Binding Path=Message}" Width="100" AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"></TextBox>-->
                        
                        <GridViewColumn.CellTemplate>
                            
                            <DataTemplate>
                                <TextBox Text="{Binding Path=Header}" Width="300" AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="30" IsReadOnly="True"></TextBox>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                        
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <!--<TextBlock Text="{Binding ElementName=cmbStatus, Path=SelectedItem}"/>-->
    </Grid>
</Page>
